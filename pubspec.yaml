name: flutter_firebase_with_bloc
description: A project of work arount flutter and firebase with bloc pattern

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  cloud_firestore: ^0.13.3
  firebase_ml_vision: ^0.9.3+5
  image_picker: ^0.6.3+3
  rxdart: ^0.23.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true